import Foundation

/// View model struct for the vanilla collection view list cells in TrackedTVShowListView's collection view
struct TrackedTVShowListCellViewModel: Hashable {
	let text: String
	let imageName: String
}
